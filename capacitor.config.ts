import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cristians.guia1',
  appName: 'parcial2',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
